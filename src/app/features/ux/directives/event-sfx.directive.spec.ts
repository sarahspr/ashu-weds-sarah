import { EventSfxDirective } from './event-sfx.directive';

describe('EventSfxDirective', () => {
  it('should create an instance', () => {
    const directive = new EventSfxDirective();
    expect(directive).toBeTruthy();
  });
});
